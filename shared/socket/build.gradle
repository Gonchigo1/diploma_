dependencies {
    // spring boot
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // other
    implementation "io.socket:socket.io-client:${project.ext.socketIoClientVersion}"

    // shared
    api project(':shared:model')
}
